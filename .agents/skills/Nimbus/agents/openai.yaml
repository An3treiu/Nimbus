interface:
  display_name: "Nimbus"
  short_description: "Repo-specific patterns and workflows for Nimbus"
  default_prompt: "Use the Nimbus repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true